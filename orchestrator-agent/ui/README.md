# Orchestrator Agent UI

React + TypeScript (Vite) SPA for the Orchestrator agent.

Build output is written to `../backend/src/orchestrator_agent/static` and served by FastAPI.

```bash
npm install
npm run dev      # proxies /ingest and /api to backend :8090
npm run build
```
