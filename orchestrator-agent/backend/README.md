# Orchestrator Agent Backend

Python A2A FastAPI + LangGraph service. See [../README.md](../README.md).

```bash
uv sync
uv run orchestrator-agent
```

Build the UI first: `cd ../ui && npm run build`.
