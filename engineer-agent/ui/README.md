# Engineer Agent UI

React + TypeScript (Vite) SPA for the Engineer agent fleet.

Build output is written to `../backend/src/engineer_agent/static` and served by FastAPI.

```bash
npm install
npm run dev      # proxies /ingest and /api to backend :8091
npm run build
```
